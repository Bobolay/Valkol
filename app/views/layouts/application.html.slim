html.no-js lang="en" 
  head
    meta charset="utf-8" 
    meta content="width=device-width, initial-scale=1.0" name="viewport" 
    title Валкол
    // fancy box
    link href="/assets/jquery.fancybox.css" media="screen" rel="stylesheet"
    link href="/assets/main.css" rel="stylesheet"
    // google font
    link href="https://fonts.googleapis.com/css?family=Fira+Sans:500,700italic,700,500italic,400italic,400,300italic,300&subset=latin,cyrillic-ext" rel="stylesheet" type="text/css" 
    script src="/assets/modernizr.js" 
    // jquery
    script src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.min.js" 
    // bx slider
    link href="/assets/jquery.bxslider.css" rel="stylesheet"
    // google maps
    script src="http://maps.googleapis.com/maps/api/js"
  body
    = render "header"
    == yield
    = render "footer"
    script src="/assets/jquery.js" 
    script src="/assets/foundation/foundation.js" 
    script src="/assets/foundation/foundation.topbar.js" 
    script src="/assets/foundation.min.js"
    script src="/assets/foundation/foundation.tab.js" 
    script src="/assets/jquery.bxslider.min.js"
    script src="/assets/fancy/jquery.fancybox.pack.js"
    script src="/assets/fancy/jquery.mousewheel-3.0.6.pack.js"
    javascript:
      $(document).ready(function(){
        $('.bxslider').bxSlider({
          nextSelector: '.slider-next',
          prevSelector: '.slider-prev',
          nextText: '>',
          prevText: '<',
          pager: true,
          auto: true,
          pause: 5000
        });
      });
    javascript:
      $(document).ready(function(){
        $('.slider4').bxSlider({
          slideWidth: 300,
          minSlides: 4,
          maxSlides: 4,
          moveSlides: 1,
          slideMargin: 10
        });
      });
    javascript:
      $(document).ready(function() {
          $(document).foundation();
      });
    javascript:
      function initialize() {
        var styles = [
          {
            stylers: [
              { saturation: -100 }
            ]
          }
        ];
        var styledMap = new google.maps.StyledMapType(styles, {name: "Styled Map"});
        var mapOptions = {
            zoom: 15,
            center: new google.maps.LatLng(49.830421, 24.0309108),
            panControl:false,
            zoomControl:false,
            mapTypeControl:false,
            scaleControl:false,
            streetViewControl:false,
            overviewMapControl:false,
            rotateControl:false,
            mapTypeControlOptions:{
              mapTypeIds: [google.maps.MapTypeId.ROADMAP, "map_style"]
            }
         };
        var map = new google.maps.Map(document.getElementById('location-canvas'),
      mapOptions);
        var marker = new google.maps.Marker({
            map: map,
            draggable: true,
            position: new google.maps.LatLng(49.830421, 24.0309108)
        });
        map.mapTypes.set('map_style', styledMap);
        map.setMapTypeId('map_style');
      }
      google.maps.event.addDomListener(window, 'resize', initialize);
      google.maps.event.addDomListener(window, 'load', initialize)
    javascript:
      $(document).ready(function() {
            $('.fancybox').fancybox();
        });